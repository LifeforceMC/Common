package dev.piny.lifeforce.common.function;

import dev.piny.lifeforce.common.LifeforceCommon;
import org.bukkit.event.Listener;

/**
 * FunctionReplacer stub.
 * TODO: Implement fixed logic migrated from Legacy once the bug is identified.
 */
public class FunctionReplacer implements Listener {

    public FunctionReplacer(LifeforceCommon plugin) {
        // Register as a listener only when actual handlers are implemented.
        // plugin.getServer().getPluginManager().registerEvents(this, plugin);
    }
}